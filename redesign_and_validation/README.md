# Redesign and validation module

Run `main.m` in this folder after completing the baseline and topology optimization modules.

This module interprets the optimized density map into the First Clean Redesign, validates that clean geometry with the same FEM load case, and exports final design comparison figures. 
